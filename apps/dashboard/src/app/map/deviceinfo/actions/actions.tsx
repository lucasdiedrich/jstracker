import './actions.module.scss';

/* eslint-disable-next-line */
export interface ActionsProps {}

export function Actions(props: ActionsProps) {
  return (
    <div>
      <h1>Actions to run for the selected device!</h1>
    </div>
  );
}

export default Actions;
